exec swayidle -w \
    timeout 300 '~/.local/bin/lock' \
    timeout 400 'swaymsg "output * power off"' \
        resume  'swaymsg "output * power on"' \
    before-sleep '~/.local/bin/lock -f' \
    lock         '~/.local/bin/lock -f'
