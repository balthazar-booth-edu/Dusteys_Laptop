# Full screen → clipboard
bindsym Print exec grim - | wl-copy

# Region select → clipboard
bindsym Ctrl+Print exec grim -g "$(slurp)" - | wl-copy

# Region select → file
bindsym Ctrl+Shift+Print exec grim -g "$(slurp)" ~/Pictures/screenshot-$(date +%Y%m%d-%H%M%S).png
